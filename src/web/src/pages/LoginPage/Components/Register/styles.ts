import styled from 'styled-components';

export const TermsOfUse = styled.p`
    margin: 0.5rem 2.0313rem 0;
    text-align: center;
    font: 300 0.5rem 'Roboto';
    color: #000000;
    letter-spacing: 0.02em;

    a {
        font-weight: 500;
        color: var(--info-v1);
    }
`;