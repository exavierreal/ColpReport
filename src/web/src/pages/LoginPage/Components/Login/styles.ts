import styled from 'styled-components';

export const ForgotPassword = styled.p`
    margin: 0.5rem 2.0313rem 0;
    text-align: center;
    font: 500 0.5rem 'Roboto';
    color: var(--danger-v1);
    letter-spacing: 0.02em;

    &:hover {
        text-decoration: underline;
        cursor: pointer;
    }
`;